@using MajorSelection.Models.ViewModel

@{
    var notification = (MyNotification)ViewData["Notification"];
    if (notification != null)
    {
        var notificationClass = "";
        switch (notification.Type)
        {
            case MajorSelection.Enums.NotificationType.Success:
                notificationClass = "alert-success";
                break;
            case MajorSelection.Enums.NotificationType.Info:
                notificationClass = "alert-secondary";
                break;
            case MajorSelection.Enums.NotificationType.Error:
                notificationClass = "alert-danger";
                break;
            default:
                notificationClass = "";
                break;
        }

        <div class="container mb-5">
            <div class="alert @notificationClass alert-dismissible fade show mb-5" role="alert">
                @Html.Raw(notification.Message)
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    }
}